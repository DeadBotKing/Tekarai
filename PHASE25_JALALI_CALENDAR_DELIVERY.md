# فاز ۲۵ — تاریخ شمسی (جلالی) در سراسر رابط

## چه چیزی اضافه شد؟
نمایش و **انتخاب** تاریخ به‌صورت **شمسی (جلالی)** با ارقام و نام ماه‌های فارسی، در کل رابط:

- **نمایش تاریخ‌ها:** همه‌ی تاریخ‌ها به‌جای میلادی، شمسی نشان داده می‌شوند — مثل «۲ مهر ۱۴۰۵» یا «۱۴۰۵/۰۷/۰۲» و تاریخ‌های همراه با ساعت مثل «۲ مهر ۱۴۰۵ – ۱۴:۳۰».
- **انتخاب تاریخ:** یک **تقویم شمسی** جای فیلدهای `type="date"` مرورگر نشست؛ کاربر روی تقویم فارسی روز را انتخاب می‌کند (با ناوبری ماه، دکمه‌ی «امروز» و «پاک کردن»).

## نکته‌ی مهم معماری
تمام تاریخ‌ها **همچنان به‌صورت میلادی ISO (`YYYY-MM-DD`) با بک‌اند رد و بدل می‌شوند** — یعنی هیچ تغییری در قرارداد API و دیتابیس ایجاد نشد. شمسی‌سازی فقط در لایه‌ی نمایش/ورودیِ فرانت انجام می‌شود:
- نمایش: میلادی → شمسی هنگام رندر.
- انتخاب: کاربر شمسی انتخاب می‌کند → به میلادی ISO تبدیل و به بک‌اند ارسال می‌شود.

تبدیل با یک ماژول **بدون هیچ وابستگی خارجی** انجام می‌شود (الگوریتم استاندارد Borkowski، همان ریاضیاتِ کتابخانه‌ی مشهور `jalaali-js`)، درون‌سازی‌شده تا build کاملاً خوداتکا و تست‌پذیر بماند.

## فایل‌های جدید
- `frontend-web/src/core/localization/jalali.ts` — هسته‌ی تبدیل میلادی↔جلالی + فرمت‌کننده‌ی فارسی + ابزار ارقام فارسی و نام ماه‌ها.
- `frontend-web/src/shared/components/JalaliDatePicker.tsx` — کامپوننت تقویم شمسی (RTL، ناوبری ماه، امروز، پاک کردن) که مقدار ISO میلادی می‌گیرد و می‌دهد.
- `frontend-web/src/tests/jalali.test.ts` — ۶ تست واحد برای صحت تبدیل و فرمت.
- استایل تقویم در `frontend-web/src/styles/globals.css` (بلوک `.jdp__*`).

## جاهایی که به‌روزرسانی شد
**نمایش تاریخ (شمسی):**
- صفحه‌ی تایم‌لاین دستگاه، گزارش نگهداری دستگاه (تاریخ تولید + جدول درخواست‌ها)، درخواست‌های کار (SLA + تاریخچه)، داشبورد نگهداری (برچسب محور نمودارها)، دستگاه‌ها (PM بعدی)، پروژه‌ها و وظایف (سررسیدها).

**انتخاب تاریخ (تقویم شمسی جایگزین `type="date"`):**
- گزارش دستگاه: بازه‌ی «از تاریخ / تا تاریخ».
- دستگاه‌ها: تاریخ انجام PM.
- پروژه‌ها: تاریخ سررسید.
- وظایف: تاریخ سررسید.

## تست‌ها — همه سبز
- **فرانت:** `npm run typecheck` سبز · `npm run test` **۲۱ تست سبز** (شامل ۶ تست جدید تبدیل جلالی) · `npm run build` موفق.
- **بک‌اند:** تغییری نکرد (قرارداد تاریخ‌ها میلادی ISO باقی ماند)؛ نیازی به مهاجرت/migration جدید نیست.
- **پیش‌نمایش زنده:** لاگین و رندر تاریخ‌ها end-to-end تأیید شد.

## اجرا روی ویندوز (بدون تغییر نسبت به قبل)
چون این فاز فقط فرانت است و migration جدیدی ندارد، کافی است پس از باز کردن زیپ، فرانت را دوباره build/dev کنید:
```powershell
# بک‌اند (مثل قبل، با Waitress)
cd C:\Users\Mitra\Desktop\Tekarai\backend
.\venv\Scripts\Activate.ps1
$env:DJANGO_SETTINGS_MODULE = "config.settings.development"
waitress-serve --listen=0.0.0.0:8000 config.wsgi:application

# فرانت (پنجره‌ی دیگر)
cd C:\Users\Mitra\Desktop\Tekarai\frontend-web
npm install
npm run dev
```
سپس هر جای رابط که تاریخ هست، شمسی نمایش داده می‌شود و انتخاب تاریخ با تقویم فارسی انجام می‌شود.
