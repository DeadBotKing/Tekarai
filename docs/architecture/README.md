# docs/architecture

**Purpose:** System, layer, module and dependency architecture documents.

**Filled in:** Phase 02 — this folder is created in Phase 01 as required
structure. Content must not be invented early; each phase delivers exactly
what its specification requires.
